import { RoutePlaceholder } from "@/components/placeholders/route-placeholder";

export default function AdminReportsPage() {
  return <RoutePlaceholder eyebrow="Admin reports" title="Privacy-safe reports" description="Route placeholder for active users, check-ins, progress, engagement, and hidden-name summaries." />;
}
