import { RoutePlaceholder } from "@/components/placeholders/route-placeholder";

export default function AdminContentPage() {
  return <RoutePlaceholder eyebrow="Admin content" title="Content management" description="Route placeholder for workbook/book files, videos, safety resources, publish state, and knowledge-base sync status." />;
}
