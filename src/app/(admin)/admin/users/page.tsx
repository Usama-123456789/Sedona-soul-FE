import { RoutePlaceholder } from "@/components/placeholders/route-placeholder";

export default function AdminUsersPage() {
  return <RoutePlaceholder eyebrow="Admin users" title="Pseudonymized users" description="Route placeholder for hidden-name user summaries without raw private journal exposure." />;
}
